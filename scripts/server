#!/bin/bash
set -e

function usage() {
    echo -n \
"Usage: $(basename "$0")
Use Docker Compose to start the app services
Example: ./scripts/console
"
}

if [ "${BASH_SOURCE[0]}" = "${0}" ]
then
    case "${1}" in
        --help)
            usage
            ;;
        *)
            docker-compose up -d franklin
            ;;
    esac
fi
